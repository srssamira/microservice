spring:
  application:
    name: consumer-api
  h2:
    console:
      enable: true
  datasource:
    url: jdbc:h2:file:./data/consumerdb
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

server:
  port: 8081